import React from 'react';
import { ScrollView, View, Text} from 'react-native';

const home = () => {
  return (
    <ScrollView>
    <View>
      <Text>home</Text>
    </View>
    </ScrollView>
  );
};
export default home;
