import React from 'react';
import { ScrollView, View, Text} from 'react-native';

const settings = () => {
  return (
    <ScrollView>
    <View>
      <Text>settings</Text>
    </View>
    </ScrollView>
  );
};
export default settings;
